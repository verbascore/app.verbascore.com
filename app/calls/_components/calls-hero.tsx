import { PageHeader } from "@/components/page-header";

export function CallsHero() {
  return (
    <PageHeader
      eyebrow="Sales Calls"
      title="Call Library"
      description="Upload dual-channel MP3s, trigger AI analysis, and review every seller conversation from a single shared workspace."
    />
  );
}
